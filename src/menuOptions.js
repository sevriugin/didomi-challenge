import Option from './models/Option';

const menuOptions = [
    new Option('New', '/give-consent', 'addBoxIcon', 'Add new Consent'),
    new Option('Collected', '/consents', 'tocIcon', 'Consents'),
];

export default menuOptions;